program projectc;

var a: integer;
begin
repeat
writeln('zadej libovolne kladne cislo a ziskas hodnotu v Ascii tabulce :');
readln(a);
if a<0 then begin
      writeln('zkus to znovu a lepe');
      end else begin
      writeln('znakem je: ',chr(a));
    end;

until a>0;
readln();

end.         
